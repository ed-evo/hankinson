// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ed-evo/hankinson/server/ent/capitolo"
)

// Capitolo is the model entity for the Capitolo schema.
type Capitolo struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Nome holds the value of the "nome" field.
	Nome string `json:"nome,omitempty"`
	// MinNumeroDomanda holds the value of the "min_numero_domanda" field.
	MinNumeroDomanda int `json:"min_numero_domanda,omitempty"`
	// MaxNumeroDomanda holds the value of the "max_numero_domanda" field.
	MaxNumeroDomanda int `json:"max_numero_domanda,omitempty"`
	// TotaleDomande holds the value of the "totale_domande" field.
	TotaleDomande int `json:"totale_domande,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CapitoloQuery when eager-loading is set.
	Edges        CapitoloEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CapitoloEdges holds the relations/edges for other nodes in the graph.
type CapitoloEdges struct {
	// Domande holds the value of the domande edge.
	Domande []*Domanda `json:"domande,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DomandeOrErr returns the Domande value or an error if the edge
// was not loaded in eager-loading.
func (e CapitoloEdges) DomandeOrErr() ([]*Domanda, error) {
	if e.loadedTypes[0] {
		return e.Domande, nil
	}
	return nil, &NotLoadedError{edge: "domande"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Capitolo) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case capitolo.FieldID, capitolo.FieldMinNumeroDomanda, capitolo.FieldMaxNumeroDomanda, capitolo.FieldTotaleDomande:
			values[i] = new(sql.NullInt64)
		case capitolo.FieldNome:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Capitolo fields.
func (_m *Capitolo) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case capitolo.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case capitolo.FieldNome:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field nome", values[i])
			} else if value.Valid {
				_m.Nome = value.String
			}
		case capitolo.FieldMinNumeroDomanda:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field min_numero_domanda", values[i])
			} else if value.Valid {
				_m.MinNumeroDomanda = int(value.Int64)
			}
		case capitolo.FieldMaxNumeroDomanda:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field max_numero_domanda", values[i])
			} else if value.Valid {
				_m.MaxNumeroDomanda = int(value.Int64)
			}
		case capitolo.FieldTotaleDomande:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field totale_domande", values[i])
			} else if value.Valid {
				_m.TotaleDomande = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Capitolo.
// This includes values selected through modifiers, order, etc.
func (_m *Capitolo) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryDomande queries the "domande" edge of the Capitolo entity.
func (_m *Capitolo) QueryDomande() *DomandaQuery {
	return NewCapitoloClient(_m.config).QueryDomande(_m)
}

// Update returns a builder for updating this Capitolo.
// Note that you need to call Capitolo.Unwrap() before calling this method if this Capitolo
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Capitolo) Update() *CapitoloUpdateOne {
	return NewCapitoloClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Capitolo entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Capitolo) Unwrap() *Capitolo {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Capitolo is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Capitolo) String() string {
	var builder strings.Builder
	builder.WriteString("Capitolo(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("nome=")
	builder.WriteString(_m.Nome)
	builder.WriteString(", ")
	builder.WriteString("min_numero_domanda=")
	builder.WriteString(fmt.Sprintf("%v", _m.MinNumeroDomanda))
	builder.WriteString(", ")
	builder.WriteString("max_numero_domanda=")
	builder.WriteString(fmt.Sprintf("%v", _m.MaxNumeroDomanda))
	builder.WriteString(", ")
	builder.WriteString("totale_domande=")
	builder.WriteString(fmt.Sprintf("%v", _m.TotaleDomande))
	builder.WriteByte(')')
	return builder.String()
}

// Capitolos is a parsable slice of Capitolo.
type Capitolos []*Capitolo
