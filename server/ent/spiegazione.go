// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ed-evo/hankinson/server/ent/domanda"
	"github.com/ed-evo/hankinson/server/ent/spiegazione"
)

// Spiegazione is the model entity for the Spiegazione schema.
type Spiegazione struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// NumeroDomanda holds the value of the "numero_domanda" field.
	NumeroDomanda int `json:"numero_domanda,omitempty"`
	// Spiegazione holds the value of the "spiegazione" field.
	Spiegazione string `json:"spiegazione,omitempty"`
	// FocusLinguistico holds the value of the "focus_linguistico" field.
	FocusLinguistico string `json:"focus_linguistico,omitempty"`
	// RegolaChiave holds the value of the "regola_chiave" field.
	RegolaChiave string `json:"regola_chiave,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SpiegazioneQuery when eager-loading is set.
	Edges        SpiegazioneEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SpiegazioneEdges holds the relations/edges for other nodes in the graph.
type SpiegazioneEdges struct {
	// Domanda holds the value of the domanda edge.
	Domanda *Domanda `json:"domanda,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DomandaOrErr returns the Domanda value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SpiegazioneEdges) DomandaOrErr() (*Domanda, error) {
	if e.Domanda != nil {
		return e.Domanda, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: domanda.Label}
	}
	return nil, &NotLoadedError{edge: "domanda"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Spiegazione) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case spiegazione.FieldID, spiegazione.FieldNumeroDomanda:
			values[i] = new(sql.NullInt64)
		case spiegazione.FieldSpiegazione, spiegazione.FieldFocusLinguistico, spiegazione.FieldRegolaChiave:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Spiegazione fields.
func (_m *Spiegazione) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case spiegazione.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case spiegazione.FieldNumeroDomanda:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field numero_domanda", values[i])
			} else if value.Valid {
				_m.NumeroDomanda = int(value.Int64)
			}
		case spiegazione.FieldSpiegazione:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field spiegazione", values[i])
			} else if value.Valid {
				_m.Spiegazione = value.String
			}
		case spiegazione.FieldFocusLinguistico:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field focus_linguistico", values[i])
			} else if value.Valid {
				_m.FocusLinguistico = value.String
			}
		case spiegazione.FieldRegolaChiave:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field regola_chiave", values[i])
			} else if value.Valid {
				_m.RegolaChiave = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Spiegazione.
// This includes values selected through modifiers, order, etc.
func (_m *Spiegazione) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryDomanda queries the "domanda" edge of the Spiegazione entity.
func (_m *Spiegazione) QueryDomanda() *DomandaQuery {
	return NewSpiegazioneClient(_m.config).QueryDomanda(_m)
}

// Update returns a builder for updating this Spiegazione.
// Note that you need to call Spiegazione.Unwrap() before calling this method if this Spiegazione
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Spiegazione) Update() *SpiegazioneUpdateOne {
	return NewSpiegazioneClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Spiegazione entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Spiegazione) Unwrap() *Spiegazione {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Spiegazione is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Spiegazione) String() string {
	var builder strings.Builder
	builder.WriteString("Spiegazione(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("numero_domanda=")
	builder.WriteString(fmt.Sprintf("%v", _m.NumeroDomanda))
	builder.WriteString(", ")
	builder.WriteString("spiegazione=")
	builder.WriteString(_m.Spiegazione)
	builder.WriteString(", ")
	builder.WriteString("focus_linguistico=")
	builder.WriteString(_m.FocusLinguistico)
	builder.WriteString(", ")
	builder.WriteString("regola_chiave=")
	builder.WriteString(_m.RegolaChiave)
	builder.WriteByte(')')
	return builder.String()
}

// Spiegaziones is a parsable slice of Spiegazione.
type Spiegaziones []*Spiegazione
