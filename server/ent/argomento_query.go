// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ed-evo/hankinson/server/ent/argomento"
	"github.com/ed-evo/hankinson/server/ent/domanda"
	"github.com/ed-evo/hankinson/server/ent/predicate"
)

// ArgomentoQuery is the builder for querying Argomento entities.
type ArgomentoQuery struct {
	config
	ctx         *QueryContext
	order       []argomento.OrderOption
	inters      []Interceptor
	predicates  []predicate.Argomento
	withDomande *DomandaQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ArgomentoQuery builder.
func (_q *ArgomentoQuery) Where(ps ...predicate.Argomento) *ArgomentoQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ArgomentoQuery) Limit(limit int) *ArgomentoQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ArgomentoQuery) Offset(offset int) *ArgomentoQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ArgomentoQuery) Unique(unique bool) *ArgomentoQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ArgomentoQuery) Order(o ...argomento.OrderOption) *ArgomentoQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryDomande chains the current query on the "domande" edge.
func (_q *ArgomentoQuery) QueryDomande() *DomandaQuery {
	query := (&DomandaClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(argomento.Table, argomento.FieldID, selector),
			sqlgraph.To(domanda.Table, domanda.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, argomento.DomandeTable, argomento.DomandePrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Argomento entity from the query.
// Returns a *NotFoundError when no Argomento was found.
func (_q *ArgomentoQuery) First(ctx context.Context) (*Argomento, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{argomento.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ArgomentoQuery) FirstX(ctx context.Context) *Argomento {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Argomento ID from the query.
// Returns a *NotFoundError when no Argomento ID was found.
func (_q *ArgomentoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{argomento.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ArgomentoQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Argomento entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Argomento entity is found.
// Returns a *NotFoundError when no Argomento entities are found.
func (_q *ArgomentoQuery) Only(ctx context.Context) (*Argomento, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{argomento.Label}
	default:
		return nil, &NotSingularError{argomento.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ArgomentoQuery) OnlyX(ctx context.Context) *Argomento {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Argomento ID in the query.
// Returns a *NotSingularError when more than one Argomento ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ArgomentoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{argomento.Label}
	default:
		err = &NotSingularError{argomento.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ArgomentoQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Argomentos.
func (_q *ArgomentoQuery) All(ctx context.Context) ([]*Argomento, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Argomento, *ArgomentoQuery]()
	return withInterceptors[[]*Argomento](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ArgomentoQuery) AllX(ctx context.Context) []*Argomento {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Argomento IDs.
func (_q *ArgomentoQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(argomento.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ArgomentoQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ArgomentoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ArgomentoQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ArgomentoQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ArgomentoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ArgomentoQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ArgomentoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ArgomentoQuery) Clone() *ArgomentoQuery {
	if _q == nil {
		return nil
	}
	return &ArgomentoQuery{
		config:      _q.config,
		ctx:         _q.ctx.Clone(),
		order:       append([]argomento.OrderOption{}, _q.order...),
		inters:      append([]Interceptor{}, _q.inters...),
		predicates:  append([]predicate.Argomento{}, _q.predicates...),
		withDomande: _q.withDomande.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithDomande tells the query-builder to eager-load the nodes that are connected to
// the "domande" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ArgomentoQuery) WithDomande(opts ...func(*DomandaQuery)) *ArgomentoQuery {
	query := (&DomandaClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withDomande = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Nome string `json:"nome,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Argomento.Query().
//		GroupBy(argomento.FieldNome).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ArgomentoQuery) GroupBy(field string, fields ...string) *ArgomentoGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ArgomentoGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = argomento.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Nome string `json:"nome,omitempty"`
//	}
//
//	client.Argomento.Query().
//		Select(argomento.FieldNome).
//		Scan(ctx, &v)
func (_q *ArgomentoQuery) Select(fields ...string) *ArgomentoSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ArgomentoSelect{ArgomentoQuery: _q}
	sbuild.label = argomento.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ArgomentoSelect configured with the given aggregations.
func (_q *ArgomentoQuery) Aggregate(fns ...AggregateFunc) *ArgomentoSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ArgomentoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !argomento.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ArgomentoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Argomento, error) {
	var (
		nodes       = []*Argomento{}
		_spec       = _q.querySpec()
		loadedTypes = [1]bool{
			_q.withDomande != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Argomento).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Argomento{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withDomande; query != nil {
		if err := _q.loadDomande(ctx, query, nodes,
			func(n *Argomento) { n.Edges.Domande = []*Domanda{} },
			func(n *Argomento, e *Domanda) { n.Edges.Domande = append(n.Edges.Domande, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ArgomentoQuery) loadDomande(ctx context.Context, query *DomandaQuery, nodes []*Argomento, init func(*Argomento), assign func(*Argomento, *Domanda)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*Argomento)
	nids := make(map[int]map[*Argomento]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(argomento.DomandeTable)
		s.Join(joinT).On(s.C(domanda.FieldID), joinT.C(argomento.DomandePrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(argomento.DomandePrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(argomento.DomandePrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*Argomento]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Domanda](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "domande" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (_q *ArgomentoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ArgomentoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(argomento.Table, argomento.Columns, sqlgraph.NewFieldSpec(argomento.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, argomento.FieldID)
		for i := range fields {
			if fields[i] != argomento.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ArgomentoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(argomento.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = argomento.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ArgomentoGroupBy is the group-by builder for Argomento entities.
type ArgomentoGroupBy struct {
	selector
	build *ArgomentoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ArgomentoGroupBy) Aggregate(fns ...AggregateFunc) *ArgomentoGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ArgomentoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ArgomentoQuery, *ArgomentoGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ArgomentoGroupBy) sqlScan(ctx context.Context, root *ArgomentoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ArgomentoSelect is the builder for selecting fields of Argomento entities.
type ArgomentoSelect struct {
	*ArgomentoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ArgomentoSelect) Aggregate(fns ...AggregateFunc) *ArgomentoSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ArgomentoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ArgomentoQuery, *ArgomentoSelect](ctx, _s.ArgomentoQuery, _s, _s.inters, v)
}

func (_s *ArgomentoSelect) sqlScan(ctx context.Context, root *ArgomentoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
