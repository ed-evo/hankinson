// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ed-evo/hankinson/server/ent/domanda"
	"github.com/ed-evo/hankinson/server/ent/predicate"
	"github.com/ed-evo/hankinson/server/ent/spiegazione"
)

// SpiegazioneQuery is the builder for querying Spiegazione entities.
type SpiegazioneQuery struct {
	config
	ctx         *QueryContext
	order       []spiegazione.OrderOption
	inters      []Interceptor
	predicates  []predicate.Spiegazione
	withDomanda *DomandaQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SpiegazioneQuery builder.
func (_q *SpiegazioneQuery) Where(ps ...predicate.Spiegazione) *SpiegazioneQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *SpiegazioneQuery) Limit(limit int) *SpiegazioneQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *SpiegazioneQuery) Offset(offset int) *SpiegazioneQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *SpiegazioneQuery) Unique(unique bool) *SpiegazioneQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *SpiegazioneQuery) Order(o ...spiegazione.OrderOption) *SpiegazioneQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryDomanda chains the current query on the "domanda" edge.
func (_q *SpiegazioneQuery) QueryDomanda() *DomandaQuery {
	query := (&DomandaClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(spiegazione.Table, spiegazione.FieldID, selector),
			sqlgraph.To(domanda.Table, domanda.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, spiegazione.DomandaTable, spiegazione.DomandaColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Spiegazione entity from the query.
// Returns a *NotFoundError when no Spiegazione was found.
func (_q *SpiegazioneQuery) First(ctx context.Context) (*Spiegazione, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{spiegazione.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *SpiegazioneQuery) FirstX(ctx context.Context) *Spiegazione {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Spiegazione ID from the query.
// Returns a *NotFoundError when no Spiegazione ID was found.
func (_q *SpiegazioneQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{spiegazione.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *SpiegazioneQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Spiegazione entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Spiegazione entity is found.
// Returns a *NotFoundError when no Spiegazione entities are found.
func (_q *SpiegazioneQuery) Only(ctx context.Context) (*Spiegazione, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{spiegazione.Label}
	default:
		return nil, &NotSingularError{spiegazione.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *SpiegazioneQuery) OnlyX(ctx context.Context) *Spiegazione {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Spiegazione ID in the query.
// Returns a *NotSingularError when more than one Spiegazione ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *SpiegazioneQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{spiegazione.Label}
	default:
		err = &NotSingularError{spiegazione.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *SpiegazioneQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Spiegaziones.
func (_q *SpiegazioneQuery) All(ctx context.Context) ([]*Spiegazione, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Spiegazione, *SpiegazioneQuery]()
	return withInterceptors[[]*Spiegazione](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *SpiegazioneQuery) AllX(ctx context.Context) []*Spiegazione {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Spiegazione IDs.
func (_q *SpiegazioneQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(spiegazione.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *SpiegazioneQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *SpiegazioneQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*SpiegazioneQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *SpiegazioneQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *SpiegazioneQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *SpiegazioneQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SpiegazioneQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *SpiegazioneQuery) Clone() *SpiegazioneQuery {
	if _q == nil {
		return nil
	}
	return &SpiegazioneQuery{
		config:      _q.config,
		ctx:         _q.ctx.Clone(),
		order:       append([]spiegazione.OrderOption{}, _q.order...),
		inters:      append([]Interceptor{}, _q.inters...),
		predicates:  append([]predicate.Spiegazione{}, _q.predicates...),
		withDomanda: _q.withDomanda.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithDomanda tells the query-builder to eager-load the nodes that are connected to
// the "domanda" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SpiegazioneQuery) WithDomanda(opts ...func(*DomandaQuery)) *SpiegazioneQuery {
	query := (&DomandaClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withDomanda = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		NumeroDomanda int `json:"numero_domanda,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Spiegazione.Query().
//		GroupBy(spiegazione.FieldNumeroDomanda).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *SpiegazioneQuery) GroupBy(field string, fields ...string) *SpiegazioneGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SpiegazioneGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = spiegazione.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		NumeroDomanda int `json:"numero_domanda,omitempty"`
//	}
//
//	client.Spiegazione.Query().
//		Select(spiegazione.FieldNumeroDomanda).
//		Scan(ctx, &v)
func (_q *SpiegazioneQuery) Select(fields ...string) *SpiegazioneSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &SpiegazioneSelect{SpiegazioneQuery: _q}
	sbuild.label = spiegazione.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SpiegazioneSelect configured with the given aggregations.
func (_q *SpiegazioneQuery) Aggregate(fns ...AggregateFunc) *SpiegazioneSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *SpiegazioneQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !spiegazione.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *SpiegazioneQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Spiegazione, error) {
	var (
		nodes       = []*Spiegazione{}
		_spec       = _q.querySpec()
		loadedTypes = [1]bool{
			_q.withDomanda != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Spiegazione).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Spiegazione{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withDomanda; query != nil {
		if err := _q.loadDomanda(ctx, query, nodes, nil,
			func(n *Spiegazione, e *Domanda) { n.Edges.Domanda = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *SpiegazioneQuery) loadDomanda(ctx context.Context, query *DomandaQuery, nodes []*Spiegazione, init func(*Spiegazione), assign func(*Spiegazione, *Domanda)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Spiegazione)
	for i := range nodes {
		fk := nodes[i].NumeroDomanda
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(domanda.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "numero_domanda" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *SpiegazioneQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *SpiegazioneQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(spiegazione.Table, spiegazione.Columns, sqlgraph.NewFieldSpec(spiegazione.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, spiegazione.FieldID)
		for i := range fields {
			if fields[i] != spiegazione.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withDomanda != nil {
			_spec.Node.AddColumnOnce(spiegazione.FieldNumeroDomanda)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *SpiegazioneQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(spiegazione.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = spiegazione.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SpiegazioneGroupBy is the group-by builder for Spiegazione entities.
type SpiegazioneGroupBy struct {
	selector
	build *SpiegazioneQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *SpiegazioneGroupBy) Aggregate(fns ...AggregateFunc) *SpiegazioneGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *SpiegazioneGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SpiegazioneQuery, *SpiegazioneGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *SpiegazioneGroupBy) sqlScan(ctx context.Context, root *SpiegazioneQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SpiegazioneSelect is the builder for selecting fields of Spiegazione entities.
type SpiegazioneSelect struct {
	*SpiegazioneQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *SpiegazioneSelect) Aggregate(fns ...AggregateFunc) *SpiegazioneSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *SpiegazioneSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SpiegazioneQuery, *SpiegazioneSelect](ctx, _s.SpiegazioneQuery, _s, _s.inters, v)
}

func (_s *SpiegazioneSelect) sqlScan(ctx context.Context, root *SpiegazioneQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
